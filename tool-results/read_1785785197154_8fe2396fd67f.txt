     1→     1→# LET'S SHINE — Worklog
     2→     2→
     3→     3→## PAGES-INTERACTIVE — Donate / Member / Contact pages
     4→     4→**Agent:** general-purpose sub-agent (PAGES-INTERACTIVE)
     5→     5→**Status:** ✅ Complete
     6→     6→**Files created:**
     7→     7→- `/home/z/my-project/src/components/pages/don.tsx` — Donation page (light premium)
     8→     8→- `/home/z/my-project/src/components/pages/espace-membre.tsx` — Member area (login / register / dashboard)
     9→     9→- `/home/z/my-project/src/components/pages/contact.tsx` — Contact page
    10→    10→
    11→    11→**Files modified:**
    12→    12→- `/home/z/my-project/src/components/providers/router-provider.tsx` — added `eslint-disable-next-line react-hooks/set-state-in-effect` to `setPage` call inside the hash-restore `useEffect` (matches the existing pattern in `language-provider.tsx`). This was blocking `bun run lint` from passing — pre-existing issue, not introduced by this work.
    13→    13→
    14→    14→### Implementation summary
    15→    15→All 3 pages follow the agreed conventions:
    16→    16→- `"use client"` directive, `useLanguage()` + `useLocalized()` hooks, `useRouter()` available.
    17→    17→- Top wrapper `<div className="animate-page-enter pt-20">`.
    18→    18→- Hero with `next/image` background (`fill`, `priority`, `sizes="100vw"`) + `bg-gradient-to-br from-[#0a0f1e]/85 via-[#0f172a]/80 to-[#1e3a8a]/70` overlay + white text.
    19→    19→- `SectionHeader` from `@/components/layout/section-header` with `{ badge, title, subtitle }` props.
    20→    20→- Light premium palette: `bg-shine-radial-light` section bg, white cards with `shadow-premium` / `shadow-premium-lg`, `glass-strong` for form cards.
    21→    21→- `framer-motion` entrance animations (`opacity`, `y`) with `whileInView` for cards.
    22→    22→- `lucide-react` icons throughout.
    23→    23→- `toast` from `sonner` for success messages.
    24→    24→- `btn-gold` for primary CTAs (full width on submit buttons).
    25→    25→- `input-shine` class on all text inputs.
    26→    26→- 100% responsive grids (`sm:`, `lg:` breakpoints).
    27→    27→- Localized content via `loc` (fr / en / es) for all `donationGoals[i].goal[loc]`.
    28→    28→
    29→    29→### Page-specific highlights
    30→    30→**don.tsx**
    31→    31→- 3 donation goal cards (image h-32 with overlay, animated progress bar using `donationGoals[i].color`, € amounts via `toLocaleString("fr-FR")`).
    32→    32→- Form: one-time / monthly toggle (yellow active state), 6 preset amounts grid (3 cols) + custom amount input with € suffix.
    33→    33→- Payment method 3-col grid: Card / Mobile Money / Bank Transfer, each with gradient icon + Check badge on active state (`border-yellow-400/50`).
    34→    34→- Summary + `Confirmer mon don` button + 🔒 secured transaction note.
    35→    35→- On submit: `toast.success(t("donate.thank"))`.
    36→    36→
    37→    37→**espace-membre.tsx**
    38→    38→- Mode state: `"login" | "register" | "dashboard"` (starts `login`), `AnimatePresence` for transitions.
    39→    39→- Login form: email + password (with Eye/EyeOff show-hide toggle).
    40→    40→- Register form: fullName, email, phone + country (2-col), password.
    41→    41→- Submit → `toast.success` + `setMode("dashboard")`.
    42→    42→- Dashboard: gradient blue welcome banner with "AD" gold avatar, "Aïssatou Diallo", "Membre depuis 2024", logout button.
    43→    43→- 6 dashboard tiles (Profile, Trainings w/ "3 actives", Certificates w/ "2", History, Messages w/ "5 non lus", Dashboard) with gradient icons + hover lift.
    44→    44→- Recent activity card with 3 items (Award, BookOpen, MessageSquare).
    45→    45→
    46→    46→**contact.tsx**
    47→    47→- LEFT: 4 contact cards (Address/Phone/WhatsApp/Email with gradient icons), map placeholder (h-64) with animated MapPin (`y: [0, -10, 0]` + ping ring + HQ address card at bottom), 6 social buttons (Facebook/LinkedIn/Instagram/YouTube/TikTok/X).
    48→    48→- RIGHT: contact form (name + email 2-col, subject, textarea 6 rows) + "Réponse sous 48h ouvrées" note with Clock icon + `btn-gold` submit + "Message envoyé !" success state.
    49→    49→
    50→    50→### Verification
    51→    51→- `bun run lint` → ✅ 0 errors, 0 warnings (after fixing pre-existing `router-provider.tsx` issue).
    52→    52→- `bunx tsc --noEmit` → no errors in `src/components/pages/` (only unrelated pre-existing errors in `examples/` and `skills/`).
    53→    53→- Replaced `w-4.5 h-4.5` with `w-5 h-5` in espace-membre.tsx activity icon for safety (non-standard Tailwind class).
    54→    54→
    55→    55→### Next actions for wiring agent
    56→    56→The 3 page components export `DonPage`, `EspaceMembrePage`, and `ContactPage` respectively. They are not yet wired into the router switch (likely in `src/app/page.tsx` or a route renderer). The next agent should:
    57→    57→1. Import these components where the `useRouter().page` value is switched.
    58→    58→2. Map `"donate" → <DonPage />`, `"member" → <EspaceMembrePage />`, `"contact" → <ContactPage />`.
    59→    59→3. Ensure the navbar's "Faire un don" / "Espace membre" / "Contact" links call `navigate("donate" | "member" | "contact")`.
    60→    60→
    61→    61→---
    62→    62→
    63→    63→## AUTH-FRONTEND — useAuth hook + SessionProvider
    64→    64→**Agent:** general-purpose sub-agent (AUTH-FRONTEND)
    65→    65→**Status:** ✅ Complete
    66→    66→
    67→    67→**Files created:**
    68→    68→- `/home/z/my-project/src/components/providers/session-provider.tsx` — thin client wrapper around `next-auth/react`'s `SessionProvider` (renamed to `NextAuthSessionProvider` to avoid the naming conflict with the local export).
    69→    69→- `/home/z/my-project/src/hooks/use-auth.ts` — `useAuth()` hook exposing `{ user, status, isLoading, isAuthenticated, isAdmin, login, register, logout, refresh }`.
    70→    70→
    71→    71→**Files modified:**
    72→    72→- `/home/z/my-project/src/app/layout.tsx` — wrapped the existing `<RouterProvider>` (inside `<LanguageProvider>`) with the new `<SessionProvider>`. Added the corresponding `import { SessionProvider } from "@/components/providers/session-provider";`.
    73→    73→
    74→    74→### Implementation details
    75→    75→**session-provider.tsx**
    76→    76→- Marked `"use client"` so it can be used inside the server layout.
    77→    77→- Aliased the next-auth import to `NextAuthSessionProvider` to dodge the recursive naming conflict noted in the task spec.
    78→    78→
    79→    79→**use-auth.ts**
    80→    80→- Wraps `useSession()` from `next-auth/react`. Pulls `data: session`, `status`, and `update` from the session context.
    81→    81→- `user` is cast to `{ id?, email?, name?, role? }` (or `null` when no session) — matches the shape produced by `/src/lib/auth.ts` callbacks (`token.id`, `token.role`).
    82→    82→- `login(email, password)`: calls `signIn("credentials", { email, password, redirect: false })`. On error returns `{ ok: false, error }`. On success calls `update?.()` to force the session to re-fetch and returns `{ ok: true, error: null }`.
    83→    83→- `register(data)`: POSTs to `/api/auth/register` with `{ email, password, name, phone, country }`. On non-OK response returns `{ ok: false, error: json.error }`. On OK, auto-logs-in via `signIn("credentials", …)` and calls `update?.()`.
    84→    84→- `logout()`: calls `signOut({ redirect: false })` (no client-side navigation — works with the custom `useRouter()` from `@/components/providers/router-provider`).
    85→    85→- `refresh()`: thin wrapper around `update()` so consumers can force a session refetch (e.g. after PATCH `/api/auth/me`).
    86→    86→- `isAdmin`: `user?.role === "ADMIN"` (matches the `role` field populated by the JWT callback in `/src/lib/auth.ts`).
    87→    87→- All async methods are wrapped in `useCallback` so they're referentially stable; error handling uses `instanceof Error` rather than `any` to satisfy the project's strict ESLint config (`no-explicit-any` / `no-unsafe-*` rules enforced by `eslint()` — confirmed by lint passing at exit code 0).
    88→    88→
    89→    89→### Verification
    90→    90→- `bun run lint` → ✅ exit 0, 0 errors, 0 warnings.
    91→    91→- No TypeScript errors introduced (hook types align with the `Session` shape extended by `/src/lib/auth.ts` callbacks).
    92→    92→
    93→    93→### Next actions for wiring agent
    94→    94→The auth layer is now ready to be consumed. The next agent should:
    95→    95→1. Replace the placeholder auth in `/src/components/pages/espace-membre.tsx` (which currently just does `toast.success` + `setMode("dashboard")`) with real calls to `useAuth().login(...)` / `useAuth().register(...)`.
    96→    96→2. Wire the dashboard mode gate on `isAuthenticated` — redirect unauthenticated users back to the login form (use `useRouter().navigate("member")` if needed).
    97→    97→3. Use `useAuth().user` to populate the dashboard welcome banner (replace the hardcoded "Aïssatou Diallo" / "AD" avatar).
    98→    98→4. Use `useAuth().logout()` for the logout button (currently a no-op).
    99→    99→5. Optionally add an admin-only entry in the navbar visible when `useAuth().isAdmin` is true.
   100→   100→
   101→   101→---
   102→   102→
   103→   103→## API-PUBLIC — Public API routes for content + contact + donations
   104→   104→**Agent:** general-purpose sub-agent (API-PUBLIC)
   105→   105→**Status:** ✅ Complete
   106→   106→**Files created (15 route handlers across 13 files):**
   107→   107→- `/home/z/my-project/src/app/api/programs/route.ts` — GET list (orderBy createdAt asc)
   108→   108→- `/home/z/my-project/src/app/api/programs/[slug]/route.ts` — GET single, includes `_count.registrations`
   109→   109→- `/home/z/my-project/src/app/api/formations/route.ts` — GET list, optional `?category=` (categoryFr contains) + `?popular=true`
   110→   110→- `/home/z/my-project/src/app/api/formations/[slug]/route.ts` — GET single
   111→   111→- `/home/z/my-project/src/app/api/products/route.ts` — GET list with optional `?category=` / `?featured=true` / `?search=` (name OR brand contains); POST create (ADMIN only, 403 if not admin)
   112→   112→- `/home/z/my-project/src/app/api/products/[slug]/route.ts` — GET single; PATCH update (ADMIN only); DELETE (ADMIN only)
   113→   113→- `/home/z/my-project/src/app/api/events/route.ts` — GET list (orderBy date asc)
   114→   114→- `/home/z/my-project/src/app/api/events/[slug]/route.ts` — GET single
   115→   115→- `/home/z/my-project/src/app/api/articles/route.ts` — GET list (published only, optional `?tag=`)
   116→   116→- `/home/z/my-project/src/app/api/articles/[slug]/route.ts` — GET single published article (404 if unpublished)
   117→   117→- `/home/z/my-project/src/app/api/contact/route.ts` — POST creates ContactMessage, validates name/email/subject/message, 201
   118→   118→- `/home/z/my-project/src/app/api/newsletter/route.ts` — POST subscribe: existing+active → 200 "Déjà abonné"; existing+inactive → reactivate, 201; new → create, 201
   119→   119→- `/home/z/my-project/src/app/api/partner-request/route.ts` — POST creates PartnerRequest (phone/organization optional), 201
   120→   120→- `/home/z/my-project/src/app/api/donations/route.ts` — POST creates Donation (defaults currency="EUR", status="PENDING"); GET list (ADMIN only, includes user relation)
   121→   121→- `/home/z/my-project/src/app/api/registrations/route.ts` — POST creates Registration (requires auth, userId from session, validates type + matching id)
   122→   122→
   123→   123→**Files modified:**
   124→   124→- `/home/z/my-project/worklog.md` — appended this section.
   125→   125→
   126→   126→### Implementation summary
   127→   127→All routes follow the agreed conventions:
   128→   128→- `import { NextResponse } from "next/server"; import { db } from "@/lib/db";`
   129→   129→- Admin/auth routes also import `getServerSession` from `next-auth` and `authOptions` from `@/lib/auth`.
   130→   130→- Every handler wrapped in `try/catch` with `console.error("[ROUTE_TAG]", e)` and 500 fallback.
   131→   131→- All responses use `NextResponse.json(...)`.
   132→   132→- Required fields validated → 400 if missing; not-found → 404; unauthorized → 401; forbidden → 403; created → 201.
   133→   133→- Email fields normalized to lowercase (contact / newsletter / partner-request / donations).
   134→   134→- Newsletter subscription uses `findUnique` on the `@unique` email field, returns 200 "Déjà abonné" if already active, reactivates if inactive, creates if new.
   135→   135→- Donation POST defaults `currency="EUR"` and `status="PENDING"` as specified.
   136→   136→- Registration POST requires auth, sets `userId` from `(session.user as any).id`, validates that `type` is one of `"PROGRAM"|"FORMATION"|"EVENT"` and that the corresponding `programId`/`formationId`/`eventId` is supplied, defaults `paid=false` / `amount=0` / `status="PENDING"`.
   137→   137→- Product POST requires admin + at least `slug`, `category`, `name`, `brand`, `price`, `image`; numeric fields coerced via `Number(...)`, booleans via `Boolean(...)`, missing strings default to `""` or `null`.
   138→   138→- Product PATCH only updates fields present in the body (sparse update). DELETE returns `{ success: true }`.
   139→   139→
   140→   140→### Next.js 16 async params
   141→   141→Dynamic route handlers use the Next.js 16 signature `{ params }: { params: Promise<{ slug: string }> }` with `const { slug } = await params;`. (Using the older synchronous `params: { slug: string }` caused 5 `tsc` errors in `.next/dev/types/validator.ts` — confirmed fixed after migration.) In the products PATCH handler, the awaited slug is named `paramSlug` to avoid shadowing the body's optional `slug` field used for rename operations.
   142→   142→
   143→   143→### Verification
   144→   144→- `bun run lint` → ✅ 0 errors, 0 warnings.
   145→   145→- `bunx tsc --noEmit` → ✅ 0 errors in any `src/app/api/{programs,formations,products,events,articles,contact,newsletter,partner-request,donations,registrations}/` files. The only remaining `tsc` error in `src/` is a pre-existing issue in `src/app/api/auth/me/route.ts` (Prisma `select` + `include` conflict on `User.findUnique`) — not introduced by this task; it predates the API-PUBLIC work and belongs to a separate auth agent's scope. The `examples/` and `skills/` errors are also pre-existing and unrelated.
   146→   146→
   147→   147→### Next actions for wiring agent
   148→   148→The public API surface is now complete. Frontend components (and admin pages) can call:
   149→   149→- `GET /api/programs` and `GET /api/programs/[slug]` for the programs page + detail.
   150→   150→- `GET /api/formations?category=…&popular=true` and `GET /api/formations/[slug]` for the formations catalog.
   151→   151→- `GET /api/products?category=…&featured=true&search=…` + `GET /api/products/[slug]` for the shop; admin can `POST`/`PATCH`/`DELETE` (must be logged in as a user with `role === "ADMIN"`).
   152→   152→- `GET /api/events` + `GET /api/events/[slug]` for the events calendar.
   153→   153→- `GET /api/articles?tag=…` + `GET /api/articles/[slug]` for the blog (only `published: true` items are exposed).
   154→   154→- `POST /api/contact` from the contact page form (already wired to a `toast.success` in `contact.tsx` — replace placeholder with a real `fetch`).
   155→   155→- `POST /api/newsletter` from any newsletter form (footer, etc.) — handle 200 "Déjà abonné" gracefully.
   156→   156→- `POST /api/partner-request` from the partnerships / become-a-partner form.
   157→   157→- `POST /api/donations` from `don.tsx` — replace the placeholder `toast.success` with a real `fetch`. The donation returns 201 with the created record (status `"PENDING"`, to be confirmed by a payment webhook later).
   158→   158→- `POST /api/registrations` from authenticated program/formation/event detail pages (requires session cookie).
   159→   159→- `GET /api/donations` is admin-only and can be wired into the admin dashboard's donations table.
   160→   160→
   161→   161→---
   162→   162→
   163→   163→## WIRE-FORMS — Wire form components to real API endpoints
   164→   164→**Agent:** general-purpose sub-agent (WIRE-FORMS)
   165→   165→**Status:** ✅ Complete
   166→   166→
   167→   167→**Files modified (6):**
   168→   168→- `/home/z/my-project/src/components/pages/contact.tsx` — contact form → `POST /api/contact`
   169→   169→- `/home/z/my-project/src/components/layout/footer.tsx` — newsletter form → `POST /api/newsletter`
   170→   170→- `/home/z/my-project/src/components/pages/partenaires.tsx` — partner form → `POST /api/partner-request`
   171→   171→- `/home/z/my-project/src/components/pages/don.tsx` — donation form → `POST /api/donations`
   172→   172→- `/home/z/my-project/src/components/pages/program-detail.tsx` — register button → `POST /api/registrations` (type `PROGRAM`)
   173→   173→- `/home/z/my-project/src/components/pages/formation-detail.tsx` — register button → `POST /api/registrations` (type `FORMATION`)
   174→   174→
   175→   175→### Implementation summary
   176→   176→Every form now uses a real `fetch` to its matching public API endpoint with consistent UX conventions:
   177→   177→- **Loading state**: a dedicated boolean (`sending` / `subscribing` / `submitting` / `registering`) gates the submit button via `disabled={...}` + `disabled:opacity-70 disabled:cursor-not-allowed`. Button label switches to a "in progress" message (`Envoi en cours...`, `Traitement en cours...`, `Inscription...`).
   178→   178→- **Success path**: `toast.success(...)` is fired after `res.ok`. Forms that own their inputs use `(e.target as HTMLFormElement).reset()` to clear. The contact & partner forms keep their existing success banner / state (4s timeout).
   179→   179→- **Error path**: non-OK responses → `toast.error("Erreur ...")`; thrown network errors → `toast.error("Erreur réseau")`. `try/catch` wraps every fetch.
   180→   180→- **Field→API mapping**:
   181→   181→  - `contact.tsx`: FormData with `name="name|email|subject|message"` attributes added to each input; payload `{ name, email, subject, message }` → `/api/contact`.
   182→   182→  - `footer.tsx`: input gets `name="email"`; reads the response JSON and uses `data.message || "Inscription réussie !"` so the API's "Déjà abonné" / reactivation messages bubble through.
   183→   183→  - `partenaires.tsx`: 5 fields with proper `name` attributes; payload `{ name, email, phone, subject, message }` → `/api/partner-request`.
   184→   184→  - `don.tsx`: builds payload from existing `mode` / `amount` / `custom` / `payment` state. Uses `useAuth().user` to populate `donorName` / `donorEmail` / `userId` (falls back to `"Donateur"` / `"donor@letsshine.africa"` when anonymous). Maps `payment` → `CARD` / `ORANGE_MONEY` / `BANK_TRANSFER` via a `methodMap`. Guard: `finalAmount <= 0` → `toast.error("Montant invalide")`. Success toast includes the amount: `t("donate.thank") + \` (${finalAmount}€)\``.
   185→   185→  - `program-detail.tsx` / `formation-detail.tsx`: auth-gated via `useAuth().isAuthenticated`. Unauthenticated users get `toast.error("Connectez-vous pour vous inscrire")` + `navigate("member")` to redirect them to login. Program sends `{ type: "PROGRAM", programId, amount: 0 }`; formation sends `{ type: "FORMATION", formationId, amount: formation.price }`.
   186→   186→- **Imports added**: `useState` (footer / program-detail / formation-detail — already present in contact / partenaires / don), `useAuth` from `@/hooks/use-auth` (don / program-detail / formation-detail).
   187→   187→- All edits were surgical via `MultiEdit` — no full-file rewrites; the existing motion / styling / hero / sidebar markup was left untouched.
   188→   188→
   189→   189→### Verification
   190→   190→- `bun run lint` on the 6 modified files only (`bunx eslint src/components/pages/{contact,partenaires,don,program-detail,formation-detail}.tsx src/components/layout/footer.tsx`) → ✅ 0 errors, 0 warnings.
   191→   191→- Full `bun run lint` → 4 pre-existing errors in **untouched** files (`admin.tsx`: 3 × `react-hooks/set-state-in-effect` at lines 248/358/440; `espace-membre.tsx`: 1 × same rule at line 53). These were introduced by prior agents (ADMIN-PAGES / AUTH-FRONTEND wiring work) and are **not** caused by WIRE-FORMS — confirmed by the targeted lint run above showing clean output on all 6 files I touched.
   192→   192→
   193→   193→### Next actions
   194→   194→- The remaining 4 lint errors belong to the `admin.tsx` / `espace-membre.tsx` owners (the `useEffect(() => { load(); }, [...])` pattern that calls `setState` synchronously needs either an `eslint-disable-next-line react-hooks/set-state-in-effect` comment, like the one already applied to `router-provider.tsx` / `language-provider.tsx`, or a refactor to a non-effect trigger).
   195→   195→- Once payment webhooks exist, the `don.tsx` flow can be enhanced to redirect to a payment provider URL returned by `/api/donations` (currently it returns 201 with `status: "PENDING"`).
   196→   196→- For the registration flows, the API currently creates a `PENDING` Registration — the UI could optionally poll `GET /api/registrations` (once exposed) or read the response JSON to confirm the registration was accepted and surface the next step (e.g. payment for paid formations).
   197→   197→
   198→   198→---
   199→   199→
   200→   200→## AUDIT-SYNC — Read-only audit of dashboard → public-site data flow
   201→   201→**Agent:** general-purpose sub-agent (AUDIT-SYNC)
   202→   202→**Status:** ✅ Complete (read-only audit — no code modified)
   203→   203→**Task:** Determine whether changes made in the admin dashboard automatically reflect on the public site interface.
   204→   204→
   205→   205→### 1. Executive summary
   206→   206→**The dashboard is NOT wired to the public site.** Out of **18 inspected page components**, only **2** (`admin.tsx`, `espace-membre.tsx`) consume the API. Every public-facing content page (`home`, `programs`, `program-detail`, `formations`, `formation-detail`, `boutique`, `product-detail`, `actualites`, `article-detail`, `evenements`, `services`, `partenaires`, `mediatheque`, `about`, `don`, `contact`) renders from **static TypeScript arrays** imported directly from `/src/lib/data.ts`. The API routes `/api/programs`, `/api/formations`, `/api/events`, `/api/articles` exist and read from Prisma, but **no public page ever calls them**. Worse, the admin has no UI to edit any of programs / formations / articles / events / services / partners / media / team / donation-goals — so even though the DB has those tables and a seed populates them, the admin cannot change them, and even if they could, the public pages would not reflect the change.
   207→   207→
   208→   208→**Bottom line:** editing anything in the admin (currently: user role, product stock flag) only affects DB-stored views (admin's own tables, member dashboard). It has **zero effect on the public site** because the public site ignores the DB entirely.
   209→   209→
   210→   210→### 2. Per-page audit table
   211→   211→
   212→   212→| # | Page component | Data source | Evidence (import line + usage) | Gap when DB changes |
   213→   213→|---|---|---|---|---|
   214→   214→| 1 | `home.tsx` | **STATIC** | L13: `import { stats, programs, formations, products, articles, events, caseStudies, heroGallery } from "@/lib/data";` — L23–26: `featuredPrograms = programs.slice(0,4)`, `featuredProducts = products.filter((p) => p.featured).slice(0,4)`, `featuredArticles = articles.slice(0,3)`, `featuredEvents = events.slice(0,2)`. L268: `formations.filter((f) => f.popular).slice(0,3)`. L135: `stats.map(...)`. L365 `caseStudies.map(...)`. L477 `heroGallery.map(...)`. **No `fetch(` anywhere in the file.** | Admin changes to programs/formations/products/articles/events/stats/caseStudies/gallery are **never** reflected on the home page. |
   215→   215→| 2 | `programs.tsx` | **STATIC** | L11: `import { programs } from "@/lib/data";` — L57: `programs.map((p, i) => {…})` renders the grid. No fetch. | New / edited / deleted Programs in DB are invisible. |
   216→   216→| 3 | `program-detail.tsx` | **STATIC (read) + API (write)** | L10: `import { programs } from "@/lib/data";` — L21: `const program = programs.find((p) => p.id === params.id) ?? programs[0];` — L245: sidebar `programs.filter((p) => p.id !== program.id).slice(0,4)`. L31–34: `fetch("/api/registrations", {method:"POST", body: JSON.stringify({type:"PROGRAM", programId, amount:0})})` (write-only, after auth check). | Detail content + "Autres programmes" list come from static array. Admin edits to a Program are invisible. POST registration writes to DB but the success toast is purely cosmetic — the page never re-fetches. |
   217→   217→| 4 | `formations.tsx` | **STATIC** | L23: `import { formations } from "@/lib/data";` — L32: `categories = Array.from(new Set(formations.map((f) => f.category[loc])))` — L37: `filter === "all" ? formations : formations.filter(...)`. L117: `filtered.map(...)`. No fetch. | New / edited / deleted Formations invisible on catalog. |
   218→   218→| 5 | `formation-detail.tsx` | **STATIC (read) + API (write)** | L24: `import { formations } from "@/lib/data";` — L35–36: `formations.find((f) => f.id === params.id) ?? formations[0]`. L69–71: `otherFormations = formations.filter(...).slice(0,4)`. L49–57: POST `/api/registrations` with `{type:"FORMATION", formationId, amount: formation.price}` (write-only). | Detail content + "Autres formations" sidebar from static array. DB edits invisible. |
   219→   219→| 6 | `boutique.tsx` | **STATIC** | L11: `import { products, productCategories } from "@/lib/data";` — L30: `let list = products;` filtered by `activeCat` + `query`. L39: `featured = products.filter((p) => p.featured).slice(0,4)`. L169: `productCategories.map(...)`. No fetch. | The admin's "toggle stock" PATCH on `/api/products/[slug]` updates the DB, but the boutique still shows the static `inStock` flag from `data.ts`. Newly created products (POST `/api/products`) never appear. Search filter only matches the static array. |
   220→   220→| 7 | `product-detail.tsx` | **STATIC** | L22: `import { products, productCategories } from "@/lib/data";` — L37: `products.find((p) => p.id === params.id) ?? products[0]`. L42–44: `similar = products.filter(...)`. L46–48 `handleAddToCart` is a no-op `toast.success("Ajouté au panier !")`. No fetch. | DB-level product edits (price, stock, image, gallery, specs) are invisible. Add-to-cart doesn't create an `Order`/`OrderItem`. |
   221→   221→| 8 | `actualites.tsx` | **STATIC** | L19: `import { articles } from "@/lib/data";` — L42–43: `filter === "all" ? articles : articles.filter((a) => a.tag === filter)`. L131: `filtered.map(...)`. No fetch. | New / edited / unpublished articles never reach the public list. |
   222→   222→| 9 | `article-detail.tsx` | **STATIC** | L19: `import { articles } from "@/lib/data";` — L26–27: `articles.find((a) => a.id === params.id) ?? articles[0]`. L28: `others = articles.filter(...).slice(0,3)`. L45: `article.content[loc].split("\n\n")`. No fetch. | Same gap. Articles API returns only `published:true` but is unused. |
   223→   223→| 10 | `evenements.tsx` | **STATIC** | L18: `import { events } from "@/lib/data";` — L114: `events.map((e, i) => {…})`. L59–60: `handleRegister = (title) => toast.success(\`Inscription à « ${title} » envoyée !\`)` — **pure toast, no API call** (gap vs program-detail / formation-detail which do POST `/api/registrations`). | New / edited / deleted Events invisible. Register button does not create a `Registration` row. |
   224→   224→| 11 | `services.tsx` | **STATIC** | L11: `import { services } from "@/lib/data";` — L69: `services.map(...)`. No fetch. | No `/api/services` route even exists; services are completely hardcoded. |
   225→   225→| 12 | `partenaires.tsx` | **STATIC (read) + API (write)** | L10: `import { partners, caseStudies } from "@/lib/data";` — L107: `partners.map(...)`. L159: `caseStudies.map(...)`. L37–41: `fetch("/api/partner-request", {method:"POST", body: JSON.stringify({...})})` (write-only). | Partner list, logos, tiers, sectors, case studies all hardcoded. New `PartnerRequest` rows created by the form are visible only in DB, not in admin UI. |
   226→   226→| 13 | `mediatheque.tsx` | **STATIC** | L20: `import { mediaItems, type MediaItem } from "@/lib/data";` — L28–29: `tab === "all" ? mediaItems : mediaItems.filter((m) => m.type === tab)`. L130: `filtered.map(...)`. L45–50: `downloads` is a local string array literal. No fetch. | No `/api/media` route exists. Media library is completely hardcoded. |
   227→   227→| 14 | `about.tsx` | **STATIC** | L11–12: `import { values, objectives, founder, nationalTeam, committee, experts } from "@/lib/data"; import type { TeamMember } from "@/lib/data";` — L121: `values.map(...)`, L156: `objectives.map(...)`, L187: `<Image src={founder.image} .../>`, L211: `nationalTeam.map(...)`, L217: `committee.map(...)`, L223: `experts.map(...)`. L50–54: `sections` is a local array literal (Unsplash URLs hardcoded). No fetch. | Founder, team members, committee, experts, values, objectives are all static. No `/api/about` or `/api/team` route exists. |
   228→   228→| 15 | `admin.tsx` | **API (mostly read + 2 partial writes)** | L45: `fetch("/api/admin/stats")`. L242: `fetch(\`/api/admin/users?search=...\`)` (GET). L255: `fetch("/api/admin/users", {method:"PATCH", body: JSON.stringify({userId, role})})`. L270: `fetch(\`/api/admin/users?userId=...\`, {method:"DELETE"})`. L355: `fetch("/api/products")` (GET — same route as public but used here to populate admin table). L367–371: `fetch(\`/api/products/${slug}\`, {method:"PATCH", body: JSON.stringify({inStock: !current})})`. **No create/edit product forms** — `<button>Ajouter</button>` at L382 has **no `onClick`**; the `<Edit3>` pencil at L422–425 has **no `onClick`**. `MessagesManager` (L436–458) is a stub: `load()` sets `loading=false` without fetching; the comment at L442 admits "this would need an admin messages API". | Admin is read-only except for 2 operations (toggle user role, delete user, toggle product inStock). Cannot create/edit/delete products, programs, formations, articles, events, services, partners, media, team, donation goals, orders, donations, registrations, or contact messages. |
   229→   229→| 16 | `don.tsx` | **STATIC (read) + API (write)** | L18: `import { donationGoals, donationAmounts } from "@/lib/data";` — L130: `donationGoals.map(...)`, L208: `donationAmounts.map(...)`. L46–57: `fetch("/api/donations", {method:"POST", body: JSON.stringify({donorName, donorEmail, amount, mode, method, userId})})`. | Donation goals (target/current amounts, progress bar %) are hardcoded. New `Donation` rows do not increment the `g.current` shown on the page. |
   230→   230→| 17 | `contact.tsx` | **STATIC (no DB content) + API (write)** | No `@/lib/data` import. L62–67: `contactCards` and L69–76: `socials` are local array literals. L43–47: `fetch("/api/contact", {method:"POST", body: JSON.stringify({name, email, subject, message})})`. | Page is mostly UI chrome (cards, socials). Submitted messages reach `ContactMessage` table but admin cannot view/manage them (MessagesManager is a stub). |
   231→   231→| 18 | `espace-membre.tsx` | **API** | (from saved output L46): `fetch("/api/member/dashboard").then((r) => r.json())` populates `dashboard` state when `isAuthenticated`. Login/register/logout delegate to `useAuth()` which calls `/api/auth/...`. | ✅ This is the only properly-wired page besides admin. Member dashboard reflects DB state for the logged-in user (registrations, certificates, donations, orders, messages, stats). |
   232→   232→
   233→   233→**Tally:** STATIC = 14 pages, MIXED (static read + API write) = 4 pages (`program-detail`, `formation-detail`, `partenaires`, `don`), API-only = 2 pages (`admin`, `espace-membre`). **0 public content pages read their content from the API.**
   234→   234→
   235→   235→### 3. Admin CRUD coverage
   236→   236→
   237→   237→| Entity | Admin UI? | Backend API? | DB model? | Verdict |
   238→   238→|---|---|---|---|---|
   239→   239→| **Users** | ✅ list + toggle role + delete | ✅ GET/PATCH/DELETE `/api/admin/users` | ✅ `User` | **Full** (but no create-user form in admin; new users self-register via `/api/auth/register`). |
   240→   240→| **Products** | ⚠️ list + toggle `inStock` only. "Ajouter" button is decorative (no `onClick`). Edit pencil is decorative (no `onClick`). No delete UI. | ✅ POST `/api/products`, PATCH/DELETE `/api/products/[slug]` | ✅ `Product` | **Partial.** Backend supports full CRUD; UI exposes only the stock toggle. |
   241→   241→| **Programs** | ❌ no tab, no form | ⚠️ GET only (`/api/programs`, `/api/programs/[slug]`). No POST/PATCH/DELETE. | ✅ `Program` | **No admin CRUD.** Even if the public pages called the API, admin could not edit content. |
   242→   242→| **Formations** | ❌ | ⚠️ GET only. | ✅ `Formation` | **No admin CRUD.** |
   243→   243→| **Articles** | ❌ | ⚠️ GET only. | ✅ `Article` (has `published` flag, but no toggle endpoint). | **No admin CRUD.** |
   244→   244→| **Events** | ❌ | ⚠️ GET only. | ✅ `Event` | **No admin CRUD.** (And public `evenements.tsx` register button is a pure toast — no POST to `/api/registrations` either.) |
   245→   245→| **Services** | ❌ | ❌ no route at all | ❌ no Prisma model | **Fully hardcoded** in `data.ts`. No DB presence at all. |
   246→   246→| **Partners** | ❌ | ❌ no route (PartnerRequest is write-only via public form) | ⚠️ `PartnerRequest` exists for the form, but the public `partners` list has no DB model. | **List is hardcoded.** Form submissions land in `PartnerRequest` but admin cannot view them. |
   247→   247→| **CaseStudies** | ❌ | ❌ | ❌ | **Fully hardcoded.** |
   248→   248→| **MediaItems** | ❌ | ❌ | ❌ | **Fully hardcoded.** |
   249→   249→| **Team (founder/nationalTeam/committee/experts)** | ❌ | ❌ | ❌ | **Fully hardcoded.** |
   250→   250→| **DonationGoals** | ❌ | ❌ | ❌ (Donation model has a `goal` enum field but no `DonationGoal` table) | **Fully hardcoded.** Real donations don't move the bar. |
   251→   251→| **DonationAmounts** | ❌ | ❌ | ❌ | **Fully hardcoded** (`[10,25,50,100,250,500]`). |
   252→   252→| **Stats / heroGallery** | ❌ | ❌ | ❌ | **Fully hardcoded.** |
   253→   253→| **Donations** | ⚠️ visible in `/api/admin/stats` recent list only | ✅ GET `/api/donations` exists but unused | ✅ `Donation` | Admin sees a 5-item recent list. No detail management / status change. |
   254→   254→| **Orders** | ⚠️ visible in stats recent list only | ❌ no `/api/admin/orders` route | ✅ `Order`, `OrderItem`, `Payment` | Backend models exist; no API to list/manage. |
   255→   255→| **Registrations** | ❌ | ⚠️ POST `/api/registrations` only (no GET for admin) | ✅ `Registration` | Public program/formation detail pages create them; admin cannot view/manage them. (Stats includes a count only.) |
   256→   256→| **ContactMessages** | ⚠️ count shown in stats; `MessagesManager` tab is a stub | ❌ no `/api/admin/messages` route | ✅ `ContactMessage` | Form POSTs reach DB; admin cannot list/mark-as-handled. |
   257→   257→| **NewsletterSubscription** | ❌ | ⚠️ POST `/api/newsletter` only | ✅ | No admin view. |
   258→   258→| **PartnerRequest** | ❌ | ⚠️ POST `/api/partner-request` only | ✅ | No admin view. |
   259→   259→| **Certificates** | ❌ | GET `/api/member/certificates` only (member-side) | ✅ `Certificate` | No admin issuance flow. |
   260→   260→| **Messages (User↔User)** | ❌ | GET/POST/PATCH `/api/member/messages` only (member-side) | ✅ `Message` | No admin moderation. |
   261→   261→
   262→   262→### 4. Critical gaps (DB change → no public-site reflection)
   263→   263→
   264→   264→These are the concrete places where data exists in the DB (or could be added by an admin) but the public site ignores it:
   265→   265→
   266→   266→1. **Programs**: `/api/programs` returns Prisma rows; `programs.tsx` and `program-detail.tsx` use `@/lib/data`. New program in DB → invisible.
   267→   267→2. **Formations**: same — `/api/formations` unused by `formations.tsx` / `formation-detail.tsx`.
   268→   268→3. **Products**: `/api/products` exists, `boutique.tsx` and `product-detail.tsx` use `@/lib/data`. Even the admin's own stock toggle (which writes to the DB) doesn't propagate to the public boutique. New products created via POST never appear.
   269→   269→4. **Articles**: `/api/articles?tag=…` and `/api/articles/[slug]` unused by `actualites.tsx` / `article-detail.tsx`. The `published` flag has no effect — the static array has no notion of published state.
   270→   270→5. **Events**: `/api/events` unused by `evenements.tsx`. Plus the public events register button is a pure `toast.success` with no POST — `/api/registrations` with `type:"EVENT"` is never sent.
   271→   271→6. **Stats / counters on home page**: hardcoded in `data.ts` (e.g. `{ value: 12500, suffix: "+", label: "..." }`). Real counts in DB (users, registrations, donations) are never shown to public visitors.
   272→   272→7. **Featured selections**: `featuredProducts`, `featuredArticles`, `featuredEvents`, popular formations filter — all derived from static arrays. The DB `featured` boolean on `Product` is irrelevant.
   273→   273→8. **Donation goals progress bars**: `donationGoals[i].current` is a static number; real `Donation` rows never move the bar.
   274→   274→9. **Similar/related lists** ("Autres programmes", "Autres formations", "Articles similaires" on detail pages): static-only.
   275→   275→10. **Hero gallery** on home: static Unsplash URLs.
   276→   276→11. **Case studies** on home + partners page: static.
   277→   277→12. **Media library** (`mediaItems`): static; no upload/list API.
   278→   278→13. **About page** (founder bio, team rosters, values, objectives): static; no admin form.
   279→   279→14. **Services catalog**: static; no DB model, no API.
   280→   280→15. **Partners grid + case studies**: static; `PartnerRequest` rows submitted via the form are never shown back.
   281→   281→16. **Contact info** (address, phone, WhatsApp, email, socials): hardcoded literals in `contact.tsx`; admin cannot edit.
   282→   282→17. **Newsletter / partner-request / contact-message back-offices**: forms write to DB, but admin UI doesn't expose them (MessagesManager is a stub; no Newsletter or PartnerRequest tab at all).
   283→   283→18. **Cart / checkout**: `product-detail.tsx` "Ajouter au panier" is a pure `toast.success` — no `Order` or `OrderItem` row is ever created. The entire shop purchase flow is missing.
   284→   284→19. **Event registration**: register button on `evenements.tsx` is `toast.success` only — `Registration` rows are only created for programs and formations.
   285→   285→20. **Admin "Ajouter" product button**: styled `<button>` with no `onClick`. The `POST /api/products` endpoint exists but is unreachable from the UI.
   286→   286→21. **Admin "Edit3" product pencil**: same — no `onClick`. PATCH endpoint exists but unused for product edits (only for the inStock toggle).
   287→   287→22. **Admin MessagesManager**: `load()` is `setLoading(false)` with no fetch; placeholder copy says "messages will appear here" but no `/api/admin/messages` route exists.
   288→   288→
   289→   289→### 5. API surface (verified)
   290→   290→
   291→   291→For reference, all 38 route handlers in `/src/app/api/`:
   292→   292→
   293→   293→```
   294→   294→admin/stats          GET
   295→   295→admin/users          GET, PATCH, DELETE
   296→   296→products             GET, POST
   297→   297→products/[slug]      GET, PATCH, DELETE
   298→   298→formations           GET
   299→   299→formations/[slug]    GET
   300→   300→programs             GET
   301→   301→programs/[slug]      GET
   302→   302→events               GET
   303→   303→events/[slug]        GET
   304→   304→articles             GET
   305→   305→articles/[slug]      GET
   306→   306→contact              POST
   307→   307→newsletter           POST
   308→   308→partner-request      POST
   309→   309→donations            GET (admin), POST
   310→   310→registrations        POST
   311→   311→member/dashboard     GET
   312→   312→member/messages      GET, POST, PATCH
   313→   313→member/certificates  GET
   314→   314→auth/register        POST
   315→   315→auth/me              GET, PATCH
   316→   316→auth/logout          POST
   317→   317→auth/[...nextauth]   (next-auth handlers)
   318→   318→route.ts (root)      GET  (health check)
   319→   319→```
   320→   320→
   321→   321→**Notable absences:** no POST/PATCH/DELETE for programs, formations, articles, events, services, partners, media, team, donation-goals. No `/api/admin/orders`, `/api/admin/messages`, `/api/admin/newsletter`, `/api/admin/partner-requests`, `/api/admin/registrations`, `/api/admin/donations` (list+manage). No `/api/services`, `/api/partners`, `/api/media`, `/api/about`/`/api/team`.
   322→   322→
   323→   323→### 6. Recommendations (prioritized)
   324→   324→
   325→   325→To achieve the audit's goal — admin edits reflect on the public site — the work breaks into three layers. Listed from highest impact to lowest:
   326→   326→
   327→   327→#### Tier 1 — Wire public pages to existing API (no backend work needed)
   328→   328→1. **`boutique.tsx`** → `useEffect(() => fetch("/api/products?category=…"), [activeCat, query])`. Replace the static `products` import. This is the highest-impact single change because the admin already has a partial product editor (stock toggle).
   329→   329→2. **`product-detail.tsx`** → `fetch(\`/api/products/${params.id}\`)` (or by slug) on mount; remove the static `products.find(...)`. Make sure the router passes a `slug`, not the static `id` (currently `navigate("product-detail", { id: p.id })` uses the static cuid-like id from `data.ts` — DB products use `slug` for the URL, but `id` is also a cuid; the API route is `[slug]` so this needs reconciliation).
   330→   330→3. **`programs.tsx`** → `fetch("/api/programs")`.
   331→   331→4. **`program-detail.tsx`** → `fetch(\`/api/programs/${slug}\`)` for the main content + `fetch("/api/programs")` for the "Autres programmes" sidebar.
   332→   332→5. **`formations.tsx`** → `fetch("/api/formations?category=…&popular=true")` (filters already supported by the API).
   333→   333→6. **`formation-detail.tsx`** → `fetch(\`/api/formations/${slug}\`)` + `fetch("/api/formations")` for "Autres formations".
   334→   334→7. **`actualites.tsx`** → `fetch("/api/articles?tag=…")` on filter change.
   335→   335→8. **`article-detail.tsx`** → `fetch(\`/api/articles/${slug}\`)` + `fetch("/api/articles")` for "autres articles".
   336→   336→9. **`evenements.tsx`** → `fetch("/api/events")`. Also wire the register button to `POST /api/registrations` with `type: "EVENT", eventId: e.id, amount: e.price` (matching the pattern in `program-detail.tsx`/`formation-detail.tsx`).
   337→   337→10. **`home.tsx`** → replace the 8 static imports with parallel `fetch` calls to `/api/programs?featured`, `/api/formations?popular=true`, `/api/products?featured=true`, `/api/articles`, `/api/events`, plus dedicated endpoints for stats/caseStudies/heroGallery (see Tier 3). At minimum, swap the API-backed entities first.
   338→   338→
   339→   339→#### Tier 2 — Build the missing admin CRUD (backend + UI)
   340→   340→11. Add `POST`/`PATCH`/`DELETE` to `/api/programs`, `/api/formations`, `/api/articles`, `/api/events` route handlers (mirror the existing pattern in `/api/products`).
   341→   341→12. Build admin tabs for Programs, Formations, Articles, Events (mirror `ProductsManager`): list, create-form modal, edit-form modal, delete with confirm. Wire the existing decorative "Ajouter" and `Edit3` buttons on `ProductsManager` to real forms.
   342→   342→13. Implement `MessagesManager` properly: add `GET /api/admin/messages` (list `ContactMessage`), `PATCH /api/admin/messages` (`{ id, handled: true }`), and remove the stub.
   343→   343→14. Add admin tabs for `PartnerRequest`, `NewsletterSubscription`, `Donation` (with status transitions), `Order` (with status transitions), `Registration` (approve/reject). Backend models already exist; only API + UI needed.
   344→   344→15. Add an admin "Donations goal" manager if you want the `don.tsx` progress bars to reflect real `Donation` aggregates (introduce a `DonationGoal` table or compute live aggregates per `goal` field).
   345→   345→
   346→   346→#### Tier 3 — DB-ify the currently-uncoded entities
   347→   347→16. Add Prisma models for `Service`, `Partner`, `CaseStudy`, `MediaItem`, `TeamMember`, `DonationGoal`, `Stat`, `HeroGalleryImage` (and migrate).
   348→   348→17. Seed these from the existing `data.ts` arrays (the seed script already exists; extend it).
   349→   349→18. Add public `GET` routes for each (`/api/services`, `/api/partners`, `/api/case-studies`, `/api/media`, `/api/team`, `/api/donation-goals`, `/api/stats`).
   350→   350→19. Add admin CRUD routes + UI tabs for each.
   351→   351→20. Swap each public page's static import for the API equivalent (`services.tsx`, `partenaires.tsx`, `mediatheque.tsx`, `about.tsx`, `don.tsx`, plus the `caseStudies`/`heroGallery`/`stats` blocks in `home.tsx`).
   352→   352→
   353→   353→#### Tier 4 — Functional gaps (not strictly sync, but found during audit)
   354→   354→21. Implement the cart: `product-detail.tsx` "Ajouter au panier" should `POST /api/orders` (or a cart endpoint) instead of `toast.success`. Schema already has `Order`/`OrderItem`/`Payment`.
   355→   355→22. Implement the events registration POST (see #9).
   356→   356→23. Wire the admin donations list to `GET /api/donations` (currently admin only sees the 5-item recent slice from `/api/admin/stats`).
   357→   357→24. Refresh the donation goals progress on `don.tsx` after a successful POST — currently the page doesn't re-fetch.
   358→   358→25. Add a revalidation step (e.g. `revalidatePath` or `router.refresh()` in the admin after every PATCH/POST/DELETE) so that even SSR components — if any get added later — pick up the change. (Currently all pages are `"use client"`, so a manual refetch is enough, but worth standardizing.)
   359→   359→
   360→   360→### 7. Verification of audit
   361→   361→- Read all 18 page components under `/src/components/pages/`.
   362→   362→- Read `/src/lib/data.ts` export list (27 exports — see Grep output above).
   363→   363→- Listed all 25 route folders under `/src/app/api/` and grepped their exported methods.
   364→   364→- Read `/prisma/schema.prisma` (full 385 lines).
   365→   365→- Read `/src/app/api/admin/stats/route.ts` to confirm admin read shape.
   366→   366→- Confirmed `/api/member/dashboard` is called by `espace-membre.tsx` (line 46 of the saved Read output).
   367→   367→- No code was modified; this is a read-only audit.
   368→   368→
   369→   369→
   370→   370→## API-NESTED — Trilingual nested shape for content API routes
   371→   371→**Agent:** general-purpose sub-agent (API-NESTED)
   372→   372→**Status:** ✅ Complete
   373→   373→**Scope:** API route files only. No frontend files modified.
   374→   374→
   375→   375→### Summary
   376→   376→Updated 10 API route files under `/home/z/my-project/src/app/api/` so that all list & detail GET endpoints now return data in the **nested trilingual shape** (`title: { fr, en, es }` instead of `titleFr, titleEn, titleEs`). All write endpoints (POST/PATCH/DELETE) accept the nested shape on input and flatten it before persisting to Prisma. The DB schema is unchanged (flat columns). Flattening is done inline in each route handler.
   377→   377→
   378→   378→All transformations use the existing helpers in `/home/z/my-project/src/lib/transformers.ts`:
   379→   379→`transformProgram`, `transformFormation`, `transformProduct`, `transformEvent`, `transformArticle`.
   380→   380→
   381→   381→### Files modified
   382→   382→- `src/app/api/programs/route.ts` — GET now `.map(transformProgram)`; added admin-only `POST` that flattens nested `{title, short, description, objectives, target, results, gallery}` → DB columns. Returns `{ program: transformProgram(...) }` with status 201.
   383→   383→- `src/app/api/programs/[slug]/route.ts` — GET transforms via `transformProgram`; added admin-only `PATCH` (partial nested body → flat DB fields) and `DELETE` (by slug).
   384→   384→- `src/app/api/formations/route.ts` — GET maps `transformFormation`; added admin-only `POST` flattening `{category, title, description, duration, program, mode}` etc.
   385→   385→- `src/app/api/formations/[slug]/route.ts` — GET transforms; added admin-only `PATCH` + `DELETE`.
   386→   386→- `src/app/api/products/route.ts` — GET maps `transformProduct`; existing POST refactored to accept nested `description: { fr, en, es }` (and `gallery`/`specs` arrays), still enforcing required-field validation; returns transformed shape.
   387→   387→- `src/app/api/products/[slug]/route.ts` — GET transforms; PATCH accepts nested `description` (and keeps back-compat with flat `descFr/descEn/descEs`); DELETE unchanged logically.
   388→   388→- `src/app/api/events/route.ts` — GET maps `transformEvent`; added admin-only `POST` flattening `{title, location, description}` and casting `date` → `Date`.
   389→   389→- `src/app/api/events/[slug]/route.ts` — GET transforms; added admin-only `PATCH` + `DELETE`.
   390→   390→- `src/app/api/articles/route.ts` — GET maps `transformArticle`; added admin-only `POST` flattening `{category, title, excerpt, content, authorRole}`, defaults `published=true`, `authorId` from session if not provided.
   391→   391→- `src/app/api/articles/[slug]/route.ts` — GET transforms (kept `published: true` filter for public read); added admin-only `PATCH` (can flip `published`, update nested fields) + `DELETE`.
   392→   392→
   393→   393→### Patterns applied consistently
   394→   394→1. **Admin guard** in every write handler:
   395→   395→   ```ts
   396→   396→   const session = await getServerSession(authOptions);
   397→   397→   if (!session?.user || (session.user as any).role !== "ADMIN") {
   398→   398→     return NextResponse.json({ error: "Accès refusé" }, { status: 403 });
   399→   399→   }
   400→   400→   ```
   401→   401→2. **Next.js 16 dynamic params**: `params: Promise<{ slug: string }>` + `const { slug } = await params;`
   402→   402→3. **JSON-array fields** (`gallery`, `objectives`, `results`, `specs`, `program`, `mode`) are `JSON.stringify(...)`'d before write and parsed back by the transformer on read.
   403→   403→4. **Backwards-compat**: write handlers accept either nested (`body.title.fr`) or flat (`body.titleFr`) input, defaulting via `body.title?.fr || body.titleFr || ""`.
   404→   404→5. **404 guard**: PATCH/DELETE first `findUnique` and return 404 if missing, preventing Prisma from throwing `RecordNotFound`.
   405→   405→
   406→   406→### Lint
   407→   407→`bun run lint` → exit code 0, no errors, no warnings.
   408→   408→
   409→   409→### Next actions (not in scope)
   410→   410→- Frontend pages already consuming nested shape (static data) will work unchanged once they switch to fetching from these API routes.
   411→   411→- If/when admin UIs are built, send nested shape in POST/PATCH bodies — routes will accept either form.
   412→   412→- Consider centralizing the flatten logic into `src/lib/transformers.ts` (e.g. `flattenProgramInput`) if more endpoints need to write — currently each route handles its own flattening for clarity.
   413→   413→